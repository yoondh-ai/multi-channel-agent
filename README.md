# 멀티채널 자동 포스팅 에이전트

키워드와 방향성을 입력하면 자동으로 콘텐츠를 생성하고 여러 채널에 포스팅하는 AI 에이전트

## 기능
- 키워드 기반 웹 리서치
- 채널별 맞춤 콘텐츠 생성 (블로그, 트위터)
- 자동 포스팅

## 설치
```bash
pip install -r requirements.txt
```

## 설정
`.env` 파일에 API 키 설정 필요

## 실행
```bash
streamlit run app.py
```
